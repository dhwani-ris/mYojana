frappe.listview_settings['Community meeting'] = {
	// add fields to fetch
	add_fields: [
		'name_of_the_beneficiary', 'gender', 'contact_number',
		'state_of_origin', 'fathers_name', 'education'
	],
	hide_name_column: true, // hide the last column which shows the `name`
}
